# @dybot/audio/config

Package-level defaults, schemas, provider presets, and templates for audio playback belong here.

Do not store user runtime audio settings, generated audio files, logs, device-specific secrets, or local databases in this directory.
