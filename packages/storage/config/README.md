# Storage Config

This directory is reserved for storage defaults, schema presets, and repository-level templates that ship with code.

Do not place user databases, local runtime config, logs, or secrets here.
