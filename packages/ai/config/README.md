# AI Config

This directory is reserved for AI provider defaults, model presets, prompt templates, and schema fixtures that ship with code.

Do not place API keys, tokens, proxy credentials, user persona files, logs, or runtime profiles here.
