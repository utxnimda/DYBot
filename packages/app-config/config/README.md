# App Config Package Config

This directory is reserved for runtime configuration defaults, schema templates, and profile examples that ship with code.

Current shipped test default:

- Douyu default test room: `9999`.

Do not store real user room profiles, secrets, logs, tokens, cookies, or local runtime config here. User runtime configuration belongs in the application data directory.
